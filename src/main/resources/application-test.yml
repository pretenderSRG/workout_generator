spring:
  datasource:
    url: jdbc:tc:postgresql:15:///testdb?TC_TMPFS=/testtmpfs:rw
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true